{
  "name": "sleepless",
  "version": "1.0.0",
  "private": true,
  "description": "Mac 防休眠应用（Caffeine 替代）",
  "scripts": {
    "prepare": "husky",
    "build:app": "bash scripts/build-app.sh debug",
    "build:app:release": "bash scripts/build-app.sh release",
    "test:swift": "swift test",
    "generate-icon": "node scripts/generate-icon.mjs"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "sharp": "^0.33.5"
  }
}
